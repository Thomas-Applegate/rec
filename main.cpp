#include <iostream>
#include <fstream>
#include <string>
#include <vector>

#include "named.h"

std::vector<named<std::string>> read_input(std::istream& is)
{
	std::vector<named<std::string>> ret;
	return ret;
}

int main(int argc, const char** argv)
{
	if(argc >= 2)
	{
		
	}else
	{
		
	}
	
	return 0;
}
